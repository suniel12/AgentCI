# Core dependencies — mirrors pyproject.toml [project.dependencies]
pydantic>=2.0
click>=8.0
rich>=13.0
pyyaml>=6.0
jinja2>=3.0
opentelemetry-api>=1.20
opentelemetry-sdk>=1.20
pytest>=7.0

# Optional extras (needed for full test suite)
openai>=1.0
anthropic>=0.20
langgraph>=0.1

# Dev dependencies
ruff
mypy
pytest-asyncio
python-dotenv
httpx
